@model string

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(false)
    .OptionLabel("Select Case Manager ...")
    .Filter(FilterType.Contains)
    .DataTextField("PATSUserName")
    .DataValueField("PATSUserId")   
    .BindTo((System.Collections.IEnumerable)ViewData["CMList"])
    .Value("-1")
    .ValuePrimitive(true)
)