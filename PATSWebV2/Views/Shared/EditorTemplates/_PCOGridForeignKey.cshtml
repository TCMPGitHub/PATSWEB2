@model string

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(false)
    .OptionLabel("Select Psychologist ...")
    .Filter(FilterType.Contains)
    .DataTextField("PATSUserName")
    .DataValueField("PATSUserId")   
    .BindTo((System.Collections.IEnumerable)ViewData["PCOList"])
    .Value("-1")
    .ValuePrimitive(true)
)