@model string

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(false)
    .OptionLabel("Select Social Worker ...")
    .Filter(FilterType.Contains)
    .DataTextField("PATSUserName")
    .DataValueField("PATSUserId")
    .BindTo((System.Collections.IEnumerable)ViewData["SWList"])
    .Value("-1")
    .ValuePrimitive(true)
)